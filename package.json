{
  "name": "CPAJOBS",
  "version": "1.0.0",
  "description": "CPA JOBS MVP - Cost-Per-Action job listing platform on GitHub + Cloudflare free tier",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "echo \"Development mode not configured yet\" && exit 1",
    "build": "echo \"Build not configured yet\" && exit 1",
    "deploy": "echo \"Deploy not configured yet\" && exit 1"
  },
  "keywords": ["cpa", "jobs", "listing", "cloudflare", "github"],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "18.18.0"
  }
}